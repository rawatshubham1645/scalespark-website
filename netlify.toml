# Netlify configuration for ScaleSpark site
# No build step – static HTML/CSS/JS

[build]
  publish = "."
  # No build command needed for static site

# Optional: redirect HTTP to HTTPS (Netlify does this by default)
# [[redirects]]
#   from = "http://*"
#   to = "https://:splat"
#   status = 301
#   force = true
